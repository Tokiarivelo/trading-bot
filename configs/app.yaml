# Global app configuration. Hot-reloadable.
mode: paper            # paper | live  — NEVER switch to live before Phase 9 criteria
timezone: "Europe/Paris"
symbols: [XAUUSD, XAGUSD, BTCUSD]
engine:
  enabled: true
  entry_timeframe: M5
  confirmation_timeframes: [H1, H4]
